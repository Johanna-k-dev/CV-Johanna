function categoryCompetencesPrint() {
    
    for (let i = 0; i < competences.length; i++) {

        const competenceObject = competences[i];
        const category =competenceObject.category;
        const filterButtonsContenerParent =document.getElementsByTagName('li');
        console.log(filterButtonsContenerParent);
        
        const filterButtonscontener=document
        if (category,true) {
            
            console.log(category);
            
        }
    }
}
categoryCompetencesPrint()

const filtres = competences